apiVersion: apps/v1
kind: Deployment

metadata:
  name: python-app

spec:
  replicas: 2  # Creates two Pods for high availability.

  selector:
    matchLabels:
      app: python-app

  template:
    metadata:
      labels:
        app: python-app

    spec:
      containers:
      - name: python-app
        # The Jenkins pipeline will push the built image here.
        image: 217428065218.dkr.ecr.us-east-1.amazonaws.com/python-app:latest

        ports:
        - containerPort: 5000 #Port exposed by the application inside the container.